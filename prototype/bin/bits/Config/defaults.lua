-- Any tables that you put in here will be loaded into Preferences


-------------------------
-- Tile property types --
-------------------------
-- Sets a property type for each tile. Properties of each type are hard-coded in LoadLevel()
-- 1 = walkable
-- 2 = wall	
TilePropertyType = {
	p = 1,
	f = 2
}